import React from 'react'
import { render, screen } from '@testing-library/react'
import ResultContainer from '../components/ResultContainer'

describe('ResultContainer', () => {
  it('처음에는 보여지지 않는다', () => {
  });

  it('확인버튼을 누르면 결과 텍스트가 보여진다', () => {
  });
})